"use client"
import React from 'react'
import Modal from '@/components/Modal/Modal'

export default function dashboard() {
  return (
    <div>
      <Modal onClose={() => 1 + 1} />
    </div>
  )
}

