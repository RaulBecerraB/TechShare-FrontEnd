import React from 'react'
import '@/styles/containers.css'
import {inter} from '@/services/fonts'

export default function CommonPage() {
  return (
    <div>
        <div className={`white-container ${inter.className} antialiased font-semibold `} style={{ color: '#1E2A5E', fontSize: '1.5rem' }}>
            Sample
            s
            s
            s
        </div>
    </div>
  )
}
